# Chess Tournament

program create an environment outside of internet to run chess tournament.
create a player and adjust ranking
create a tournament 
show item

#### installation

$ python -m venv env
$ source env/bin/activate
$ pip install -r requirements.txt

at the end $ deactivate will exit virtual environment
#### execute script

$ python chess_tournament.py 


#### execute flake8